/* The terms version the client actually accepted. Bump this whenever the
   Terms page changes; the version accepted is stamped on every booking, so a
   dispute is answered by the wording that was on screen that day, not by
   whatever the page says now. */
export const TERMS_VERSION = 'booking-terms-v1.0-2026-09-03'
export const PRIVACY_VERSION = 'privacy-v1.0-2026-09-03'
